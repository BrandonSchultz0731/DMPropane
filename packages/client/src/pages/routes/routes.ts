export enum ROUTES {
    HOME = "/",
    FEATURES = "features",
    PRICING = "pricing",
    CONTACT = "contact",
    LOGIN = "login",
    SIGNUP = "signup",
    DASHBOARD = "dashboard",
  };
  
export enum ROUTE_PATHS {
    HOME = "/",
    FEATURES = "/features",
    PRICING = "/pricing",
    CONTACT = "/contact",
    LOGIN = "/login",
    SIGNUP = "/signup",
    DASHBOARD = "/dashboard",
}